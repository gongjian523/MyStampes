<UserControl x:Class="MyStampes.Log.LogListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="20,0,20,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Rectangle Margin="-20,0,-20,0" Fill="White" Panel.ZIndex="0" Grid.Row="0"/>
        <TextBox Panel.ZIndex="1" Grid.Row="0" Width="100" Height="40" VerticalAlignment="Center" HorizontalAlignment="Left"/>
        <Button Panel.ZIndex="1" Grid.Row="0" Width="100" Height="40" VerticalAlignment="Center" HorizontalAlignment="Right"/>
        <ListView Grid.Row="1" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="40"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">
                            <TextBlock x:Name="dataText"/>
                            <TextBlock x:Name="briefText"/>
                            <TextBlock x:Name="priceText"/>
                            <Button x:Name="expendBtn"/>
                        </StackPanel>
                        <ListView Grid.Row="1">
                            <StackPanel>
                                <TextBlock x:Name="detailText"/>
                                <TextBlock x:Name="detailPriceText"/>
                                <TextBlock x:Name="paddding"/>
                            </StackPanel>
                        </ListView>
                        <StackPanel Grid.Row="2">
                            <TextBlock x:Name="agentText"/>
                            <TextBlock x:Name="progress"/>
                            <Button x:Name="editeBtn"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
